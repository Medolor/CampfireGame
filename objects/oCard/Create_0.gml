// Create Event
hovering = false;      // Track if the mouse is hovering over the button
clicked = false;       // Track if the button has been clicked
activate_button = function() {}; // Default, empty function (override in child objects)